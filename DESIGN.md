# 深海军蓝 · 设计规范

## 设计原则
- **轻量权威**：UI 不吵闹，信息层级清晰
- **品牌深度**：深海军蓝基底 + 紫色点缀
- **精密留白**：有呼吸感但不空旷

## 色彩系统

| Token | 色值 | 用途 |
|---|---|---|
| `BgDeep` | `#1c1e54` | 窗口主背景 |
| `BgCard` | `rgba(255,255,255,0.04)` | 卡片表面 |
| `BgCardHover` | `rgba(255,255,255,0.08)` | 卡片悬停 |
| `TextPrimary` | `#ffffff` | 主要文字 |
| `TextSecondary` | `rgba(255,255,255,0.7)` | 次要文字 |
| `TextMuted` | `rgba(255,255,255,0.45)` | 辅助信息 |
| `Purple` | `#533afd` | 主交互色、按钮 |
| `PurpleHover` | `#4434d4` | 按钮悬停 |
| `PurpleGlow` | `rgba(83,58,253,0.3)` | 紫色氛围光晕 |
| `StatusGreen` | `#15be53` | 运行中状态 |
| `ActionRed` | `#ea2261` | 停止 |
| `BorderDefault` | `rgba(255,255,255,0.08)` | 标准边框 |
| `BorderCard` | `rgba(255,255,255,0.06)` | 卡片边框 |

## 圆角
- 按钮/输入框：4px
- 卡片：6px
- 状态指示器：4px

## 阴影
- 卡片/弹出：蓝紫色调 `rgba(50,50,93,0.25) 0px 8px 24px`
- 按钮：紫色光晕 `rgba(83,58,253,0.2) 0px 4px 12px`

## 间距系统
- 基础单位：8px
- 卡片内边距：16px
- 卡片间距：8px
- 窗口边距：24px

## 布局结构
```
┌─────────────────────────┐
│  [logo] AkiMacro       ─│  ← 标题栏，右侧 ─ ✕
├─────────────────────────┤
│                         │
│     ┌─── 已停止 ───┐    │  ← 状态指示器（文字式）
│     │  ● 已停止    │    │     运行中 → 绿色
│     └──────────────┘    │
│                         │
│  ┌──────────────────┐   │
│  │ 侧键1            │   │  ← 功能卡片
│  │ 自动旋转       X1 │   │     简洁 label + badge
│  └──────────────────┘   │
│  ┌──────────────────┐   │
│  │ 侧键2            │   │
│  │ 双键宏         X2 │   │
│  └──────────────────┘   │
│                         │
│  ┌─── 错误信息 ──────┐  │  ← 错误横幅（红色调）
│  └───────────────────┘  │
│                         │
│    Settings · About      │  ← 底部链接
│                         │
│  ┌──────────────────┐   │
│  │  启 动 / 停 止    │   │  ← 主操作按钮
│  └──────────────────┘   │
│                         │
│  © 2026 AkiMacro           │  ← 版权信息
└─────────────────────────┘
```
